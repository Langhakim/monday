# 12testfor.py

# 변수선언 안하고 for 대표변수 처리
# 반복 - for,while
# 반복에서 사용되는 보조제어문 break반복탈출, continue복귀
for k in range(1, 11, 1) :
    if k == 5 : continue
    print(k, end='  ') 
    # if k == 5 : break
    
    # 보조제어 continue 문장기술

print()